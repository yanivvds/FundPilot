# Core Vanna with required integrations
vanna[fastapi,mssql,openai,chromadb]

# For loading environment variables from .env file
python-dotenv

# Supabase for authentication and user management
supabase>=2.0.0

# MS SQL database connectivity
pyodbc

# SQL parsing for read-only validation
sqlparse

# Additional useful packages
pandas
plotly
tabulate

# Web search (no API key required)
duckduckgo-search>=6.0